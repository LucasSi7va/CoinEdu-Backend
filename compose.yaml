services:
  db:
    image: postgres:15
    container_name: postgres_coinedu
    environment:
      POSTGRES_DB: coinedu
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: LucasGamer123
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
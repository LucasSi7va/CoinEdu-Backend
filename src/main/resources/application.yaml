spring:
  application:
    name: CoinEdu

  datasource:
    url: jdbc:postgresql://localhost:5432/coinedu
    driver-class-name: org.postgresql.Driver
    username: admin
    password: LucasGamer123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  elasticsearch:
    uris: http://localhost:9200

  mail:
    host: smtp.gmail.com
    port: 587
    username: coinedu64@gmail.com
    password: "lkzj bjpl ydqi yvle"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true